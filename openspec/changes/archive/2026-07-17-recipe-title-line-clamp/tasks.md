## 1. RecipeCard title

- [x] 1.1 Replace `truncate` with `line-clamp-2` on the title `<h3>` in `RecipeCard.tsx:135`

## 2. RecipeTableRow title

- [x] 2.1 Replace `truncate` with `line-clamp-2` on the title `<span>` in `RecipeTableRow.tsx:44`

## 3. Verification

- [x] 3.1 Run `npm run typecheck` and `npm run lint` in `frontend-food/`
- [ ] 3.2 Visual check: load recipe list page with a mix of short and long titles, verify both grid and table views
