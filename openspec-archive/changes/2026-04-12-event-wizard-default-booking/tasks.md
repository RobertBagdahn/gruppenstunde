## 1. Frontend — Schema Extension

- [x] 1.1 Add `_isAutoGenerated: z.boolean().optional()` to `WizardBookingOptionSchema` in `frontend/src/schemas/eventWizard.ts` (internal field, not sent to backend)

## 2. Frontend — Store Logic

- [x] 2.1 Create helper function `calculateDefaultBookingOption(startDate, endDate)` in `frontend/src/store/eventWizardStore.ts` that returns a booking option with name "Teilnahme", price = days x 15, description "Standardbeitrag pro Tag", `_isAutoGenerated: true`
- [x] 2.2 Update `updateStep3()` in store to trigger default booking option creation when `start_date` or `end_date` changes — only if no manually edited booking options exist
- [x] 2.3 Update `getCreatePayload()` to strip `_isAutoGenerated` flag from booking options before sending to backend

## 3. Frontend — Booking Step UI

- [x] 3.1 Update `StepBookingOptions.tsx` to handle pre-filled booking option — show it as editable
- [x] 3.2 When user edits an auto-generated option, set `_isAutoGenerated: false` to prevent overwrite
- [x] 3.3 Show a subtle indicator (e.g. info text) that the booking option was auto-generated based on event dates

## 4. Verification

- [x] 4.1 Test: Set start/end date in Step 3, navigate to Step 5 — default booking option should appear with correct price
- [x] 4.2 Test: Edit default booking option, go back and change dates — option should NOT be overwritten
- [x] 4.3 Test: No dates set — Step 5 should be empty
- [x] 4.4 Verify TypeScript compilation passes
